name: ServerBuilder
version: 1.0.0
description: Automates server setup and configuration.
author: Modix Team
website: https://modix.store
permissions:
- serverbuilder:view
- serverbuilder:create
frontend:
  routes:
  - path: /serverbuilder
    component: ServerBuilderPage
    permission: serverbuilder:view
  nav_items:
  - label: Server Builder
    path: /serverbuilder
    icon: Build
    permission: serverbuilder:view
backend:
  router: module_system.Optional.ServerBuilder.backend.routes:router
