{
  "category": "docker",
  "version": "1.1",
  "fields": [
    {
      "name": "Server Name",
      "key": "MODIX_SERVER_NAME",
      "type": "string",
      "default": "Project Zomboid",
      "description": "The display name for this server instance."
    },
    {
      "name": "Game Template",
      "key": "MODIX_GAME_TEMPLATE",
      "type": ["string", "null"],
      "default": null,
      "description": "The game template to use, or null if not set."
    },
    {
      "name": "Game is Steam",
      "key": "MODIX_GAME_IS_STEAM",
      "type": "boolean",
      "default": true,
      "description": "Whether the game uses Steam."
    },
    {
      "name": "Docker Image",
      "key": "DOCKER_IMAGE",
      "type": "string",
      "default": "modix-steamcmd-game-server:latest",
      "description": "The Docker image to use for the server."
    },
    {
      "name": "Game Steam ID",
      "key": "GAME_STEAM_ID",
      "type": "number",
      "default": 380870,
      "description": "The Steam App ID for Project Zomboid or other server."
    },
    {
      "name": "Custom Start Command",
      "key": "CUSTOM_START_SCRIPT",
      "type": "string",
      "default": "",
      "description": "The custom start command for the server. exsample: ./server_start.sh -debug"
    },
    {
      "name": "Game Version",
      "key": "GAME_VERSION",
      "type": "string",
      "default": "1.0",
      "description": "The version of the game."
    },
    {
      "name": "Server Port",
      "key": "SERVER_PORT",
      "type": "number",
      "default": 16261,
      "min": 1024,
      "max": 65535,
      "description": "Port used for the server."
    },
    {
      "name": "RCON Port",
      "key": "RCON_PORT",
      "type": "number",
      "default": 27015,
      "min": 1024,
      "max": 65535,
      "description": "Port used for RCON remote control."
    },
    {
      "name": "Steam Branch",
      "key": "STEAMAPP_BRANCH",
      "type": "string",
      "default": "public",
      "description": "The Steam branch to use."
    },
    {
      "name": "Steam Branch Password",
      "key": "STEAMAPP_BRANCH_PASSWORD",
      "type": ["string", "null"],
      "default": null,
      "description": "Password for the Steam branch, if required."
    },
    {
      "name": "Server Args",
      "key": "SERVER_ARGS",
      "type": "string",
      "default": "-debuglog=All",
      "description": "Additional arguments to pass to the server process."
    },
    {
      "name": "Use Named Volume",
      "key": "USE_NAMED_VOLUME",
      "type": "boolean",
      "default": true,
      "description": "Whether to use a named Docker volume."
    },
    {
      "name": "Docker Volume Name",
      "key": "DOCKER_VOLUME_NAME",
      "type": "string",
      "default": "pz_test_server_data",
      "description": "The name of the Docker volume."
    },
    {
      "name": "Docker Volume Path",
      "key": "DOCKER_VOLUME_PATH",
      "type": "string",
      "default": "/home/steam/pzserver",
      "description": "The path inside the container where the volume is mounted."
    },
    {
      "name": "Bind Mount Path",
      "key": "BIND_MOUNT_PATH",
      "type": ["string", "null"],
      "default": null,
      "description": "The host path to bind mount, or null if not used."
    },
    {
      "name": "Protocol Ports",
      "key": "protocal_ports",
      "type": "array",
      "default": [
        { "desired_container_port": 16261, "protocol": "udp" },
        { "desired_container_port": 16262, "protocol": "udp" },
        { "desired_container_port": 27015, "protocol": "tcp" }
      ],
      "description": "List of ports and protocols to expose.",
      "items": {
        "type": "object",
        "properties": {
          "desired_container_port": { "type": "number" },
          "protocol": { "type": "string", "enum": ["tcp", "udp"] }
        },
        "required": ["desired_container_port", "protocol"]
      },
      "environment_variables": [
        {
          "name": "MODIX_TEST",
          "key": "MODIX_TEST",
          "type": "string",
          "default": "TESTESTESTEST",
          "description": "Stay calm, this is just a test variable."
        }
      ]
    }
  ]
}
