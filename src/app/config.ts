// src/app/config.ts

// Centralized server URL — change this once and all pages use it
export const SERVER_URL = "https://486c5a0f25c2.ngrok-free.app/";

// Optional helper function
export function getServerUrl(): string {
  return SERVER_URL;
}
